import 'package:flutter/material.dart';

class H1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Chapter 1 Executable size"),
      ),
    );
  }
}
