import 'package:flutter/material.dart';

class H2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Chapter 2 Start-up performance"),
      ),
    );
  }
}